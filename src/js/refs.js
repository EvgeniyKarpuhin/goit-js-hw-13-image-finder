const searchForm = document.querySelector('#search-form');
const gallery = document.querySelector('.gallery');
const loadMore = document.querySelector('button[data-action="more-photo"]');

export default {
    searchForm,
    gallery,
    loadMore,
};